# Examples

- `believe.brand.json` — a live Agent-Readable Brand Profile (Believe). Fetch the
  companion `brand.json.sig` and `keys.json` from `believe-global.com/.well-known/`
  to verify the Ed25519 signature over these exact bytes.
